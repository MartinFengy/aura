# Current Task

当前项目已进入“暂停开发，准备交接”状态。

## 1. 项目目标

Aura 是一个面向英语学习场景的 Web 应用，目标是把“截图识别英文材料 -> 提取高质量词汇/短语 -> 组织任务 -> 听写练习 -> 学习轨迹记录 -> 飞书同步”串成一条完整学习链路，并在手机端也能顺畅使用。

## 2. 已完成功能

- 已完成 Supabase 登录体系接入，支持登录、登出与基础会话流转。
- 已完成任务化识别工作流：上传图片/文件后生成识别任务，并沉淀为词条数据。
- 已完成词阁、阅章、行迹、设置四个主工作区页面。
- 已完成云端数据同步主链路，手机端与电脑端不再只依赖各自本地数据。
- 已接入 Ark 模型配置与模型切换能力，前端可切换已接入模型。
- 已完成飞书同步主链路，可将词条结果同步到飞书。
- 已完成听写练习主流程，并记录正确 / 模糊 / 不认识结果。
- 已完成多轮移动端修复，包括底部导航遮挡、统计布局、删除确认、听写结果展示等。
- 已完成多轮图片识别链路优化，包括超时处理、非 JSON 返回处理、OCR 与模型结构化提取结合。

## 3. 当前正在开发的功能

当前没有继续编码中的功能，项目处于暂停状态。

但最近一个开发焦点是：

- 继续提高长图新闻类内容的识别质量
- 继续压缩词阁移动端字体与信息密度
- 继续收紧“假中文意思 / 假例句 / 碎片词条”的过滤规则

## 4. 未完成任务

### P0

- 重新验证长图新闻截图的真实识别结果，确认是否仍会产出假中文义、假例句、碎片词条
- 继续修复识别质量问题，目标是“宁可少一点，也不要假词条”
- 继续优化词阁移动端排版，尤其是长任务名、统计卡片、词条卡片标题与按钮

### P1

- 继续优化专有名词、人名、地名、机构名、头衔的提取准确率
- 继续优化听写、词阁、阅章在手机端的视觉密度
- 继续验证云端同步在跨设备删除、追加、重命名场景下的一致性
- 继续验证飞书同步字段是否满足当前学习场景

### P2

- 完善设置页的模型配置说明与错误提示
- 继续打磨异常态、空状态与用户反馈文案
- 根据真实使用情况继续优化 OCR / 模型 / 本地兜底三层策略

## 5. 当前最关键的卡点

1. 识别质量问题还没有被真实验收彻底确认
- 代码层已经收紧
- 但需要再次用真实新闻长截图上传验证

2. 词阁手机端仍然偏“大”
- 尤其是任务标题、词条卡片、统计块
- 用户仍明确反馈部分内容会换到下一行

## 6. 已知 Bug 和风险

- 长图 OCR 场景虽然已改成“优先 OCR 文本 + 模型结构化”，但仍需验证是否还有漏网碎片词条
- 识别结果质量仍受模型返回质量影响，不能只依赖提示词，必须依赖后端过滤
- 手机端样式已经过多轮调整，但仍可能存在个别机型下的换行和密度问题
- 云端同步已接入，但跨端高频增删改后的边界场景仍建议继续回归

## 7. 下一步最推荐继续做的事情

1. 用最近用户提供的同类新闻长截图再次上传，做真实识别回归
2. 如果仍有坏词条，继续加强 `src/app/api/analyze/route.ts` 的最终 entry 过滤
3. 如果识别已明显正常，再继续压缩词阁手机端字号和布局
4. 最后再回归一次手机端与电脑端的数据一致性

## 8. 最近重点修改文件

- `src/app/api/analyze/route.ts`
- `src/app/(workspace)/lexicon/page.tsx`
- `src/app/(workspace)/reading/page.tsx`
- `src/app/(workspace)/journey/page.tsx`
- `src/components/aura/app-shell.tsx`
- `src/components/aura/learning-workspace.tsx`
- `src/app/globals.css`
