# Decisions

## 1. 使用 Next.js App Router 作为统一前后端容器

原因：

- 需要把登录页、工作区页面、API Route、部署链路统一在一个项目里
- 便于围绕识别、词阁、行迹、设置做整体迭代

影响：

- 页面和接口都集中在 `src/app`
- 前端状态、服务端路由、部署行为都强依赖 Next.js App Router 结构

## 2. 使用 Supabase Auth + 云端数据同步

原因：

- 用户明确需要手机端与电脑端共享同一份学习数据
- 单纯依赖本地存储无法满足跨端一致性

影响：

- 任务、词条、听写记录不再只是本地原型数据
- 后续所有增删改都要优先验证跨端一致性

## 3. 识别结果仍按“任务”组织

原因：

- 用户实际工作流是每次上传材料形成一个学习单元
- 便于支持任务重命名、删除、加入听写范围、继续追加

影响：

- 任务与词条、听写记录、飞书同步之间存在强联动
- 删除任务和删除词条必须继续保持统计一致性

## 4. 模型切换能力放到用户端可操作配置中

原因：

- 用户希望直接在界面切换不同 Ark 模型
- 图片分析和文字生成链路都需要保留模型切换空间

影响：

- 必须持续验证前端所选模型是否真的传到后端并生效
- 需要持续回归 `effectiveModel` / `effectiveVisionModel`

## 5. 图片识别采用“三层策略”

策略：

1. 优先直接走模型识图结构化
2. 长图或超时场景先做 OCR 文本，再交给模型做结构化提取
3. 只有前两层都不够时，才使用本地 heuristic fallback

原因：

- 纯视觉模型会超时
- 纯本地 heuristic fallback 容易产出假中文意思、假例句、碎片词条

影响：

- `src/app/api/analyze/route.ts` 成为当前最关键的质量控制文件
- 后续更推荐继续强化最终 entry 过滤，而不是只强化提示词

## 6. 当前识别质量策略以“宁可少一点，也不要假条目”为优先

原因：

- 用户最不能接受的是错误词条、错误中文、错误例句
- 少量漏提取的容忍度高于错误输出的容忍度

影响：

- 相对从句碎片、新闻残片、模板例句、英文混入中文义都应优先拦截
- 后续调优时不能只追求数量

## 7. 移动端优先级已经上升到产品主路径级别

原因：

- 用户主要在手机端使用
- 多轮反馈都集中在手机端排版、按钮可见性、底部导航遮挡、字号过大

影响：

- 后续任何新增 UI 都必须先看手机端效果
- 词阁、阅章、行迹的手机端视觉密度仍要持续优化
